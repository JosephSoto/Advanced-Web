

  lineWidth(1);
  hide();


  moveTo(100,50)
  function triangle(size) {

  speed(0);
    forward(size);
    rotate(120);
    forward(size);
    rotate(120);
    forward(size);
    rotate(120);


  }

  for(var z = 0; z < 25; z++) {
	  if (y===10) {
	    move(0,100);
	    }
	  
  for(var y = 0; y < 10; y++){
  if (y===10) {
    move(150,0);
	
    }

  for(var x = 0; x < 5; x++) {
     // run this code 10 times

	  rotate(20);
	  triangle(50);
   
  } 
  }
  }




